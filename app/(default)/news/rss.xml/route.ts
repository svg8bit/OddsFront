import { getNewsCatalog } from "@/lib/news/catalog";
// XML escaping and feed construction retain the ColdMath news-feed pattern.
const xml=(value:string)=>value.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&apos;");
export async function GET(){const catalog=await getNewsCatalog();const items=catalog.articles.slice(0,50).map(article=>{const href=`https://oddsfront.com/news/${(article.countries[0]||"world").toLowerCase()}/${article.slug}`;return `<item><title>${xml(article.title)}</title><link>${xml(href)}</link><guid isPermaLink="true">${xml(href)}</guid><description>${xml(article.description)}</description><pubDate>${new Date(article.publishedAt).toUTCString()}</pubDate></item>`;}).join("");return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>OddsFront News</title><link>https://oddsfront.com/news</link><description>World news and geopolitical context</description><language>en</language>${items}</channel></rss>`,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, max-age=60, s-maxage=300"}});}
